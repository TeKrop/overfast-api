# Resolve the real client IP from X-Forwarded-For, but only when the request
# comes from a trusted proxy (TRUSTED_PROXY_CIDRS). Generated by entrypoint.sh.
# This must stay above the limit zones below: they key on $binary_remote_addr,
# which real_ip rewrites to the actual client address.
#
# Keying the zones directly on $http_x_forwarded_for instead would make rate
# limiting trivially bypassable, as the header is client-controlled and a
# random value per request yields a fresh bucket every time.
${REAL_IP_CONFIG}

# Create connection zone to limit number of connections per IP/domain
limit_conn_zone $binary_remote_addr zone=ofconn:10m;
limit_conn_status 429;

# Create request zone to introduce a rate limit per IP
limit_req_zone $binary_remote_addr zone=ofreq:10m rate=${RATE_LIMIT_PER_SECOND_PER_IP}r/s;
limit_req_status 429;

# Docker DNS for both Lua and proxy_pass dynamic resolution.
# valid=1s gives near-instant pickup of new container IPs during rolling
# deploys without needing a config reload. The DNS query overhead is
# negligible against the local docker resolver.
resolver 127.0.0.11 valid=1s ipv6=off;
resolver_timeout 2s;

# Prometheus metrics initialization (conditional on PROMETHEUS_ENABLED)
${PROMETHEUS_LUA_SHARED_DICT}
${PROMETHEUS_INIT_WORKER}

server {
  listen 80;

  # Use nginx to serve static content
  location /static {
    alias /static;
    try_files $uri =404;

    sendfile on;
    tcp_nopush on;

    expires 1d;
    add_header Cache-Control public;
    add_header Access-Control-Allow-Origin "*" always;
  }

  # Favicon
  location /favicon.png {
    alias /static/favicon.png;
  }
  location /favicon.ico {
    alias /static/favicon.ico;
  }

  # Block /metrics from public access (FastAPI metrics endpoint)
  location /metrics {
    return 404;
  }

  # Redirect trailing slashes to routes without slashes
  location ~ (?<no_slash>.+)/$ {
    return 301 $scheme://$host$no_slash;
  }

  # Main route
  location / {
    # Rate limiting instructions
    limit_conn ofconn ${MAX_CONNECTIONS_PER_IP};
    limit_req zone=ofreq burst=${RATE_LIMIT_PER_IP_BURST} nodelay;

    # Handle HTTP 429 when triggered by nginx rate limit
    error_page 429 = @limit_reached;

    # Always authorize any origin as it's a public API
    add_header Access-Control-Allow-Origin "*" always;

    # Handle OPTIONS method here
    if ($request_method = OPTIONS) {
      add_header Access-Control-Allow-Origin "*" always;
      add_header Access-Control-Allow-Methods "GET, HEAD, OPTIONS";
      add_header Access-Control-Allow-Headers "*";
      return 204;
    }

    # Use Lua script for Valkey logic
    default_type application/json;
    content_by_lua_block {
      local valkey_handler = require "valkey_handler"
      valkey_handler()
    }

    # Fallback to app if data not in cache
    error_page 404 502 504 = @fallback;
  }

  # Prometheus logging (conditional on PROMETHEUS_ENABLED)
  ${PROMETHEUS_LOG_BY_LUA}

  # FastAPI app fallback
  location @fallback {
    # As we're in fallback, we need to specify the header again
    add_header Access-Control-Allow-Origin "*" always;

    # Dynamic upstream — `set` + variable-based proxy_pass forces nginx
    # to consult the resolver on every request rather than caching the
    # IP from config load. With Docker's DNS round-robining the 'app'
    # service across all running containers, this gives us automatic
    # rolling-deploy support: spin up a second container, drop the
    # first, and within ~5s nginx resolves the new IP without reload.
    set $app_upstream "app:8080";
    proxy_pass http://$app_upstream;

    # No keepalive pool here — variable-based proxy_pass cannot use a
    # named upstream block. Each request opens a fresh connection;
    # acceptable for our QPS and avoids stale connections to dead
    # containers during rolling deploys.
    proxy_http_version 1.1;

    # Ensure headers are forwarded
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_pass_request_headers on;

    # Ensure HEAD requests are passed as GET
    proxy_method GET;

    # Tight connect timeout so a dead-instance attempt fails quickly
    # and clients that hit a stale DNS answer get retried by Caddy.
    proxy_connect_timeout 2s;
    proxy_read_timeout 30s;
    proxy_send_timeout 10s;
  }

  # nginx rate limit reached
  location @limit_reached {
    add_header Access-Control-Allow-Origin "*" always;
    add_header ${RETRY_AFTER_HEADER} 1 always;

    default_type application/json;
    return 429 '{"error": "API rate limit reached, please wait for 1 second before retrying"}';
  }
}

# Prometheus metrics server (conditional on PROMETHEUS_ENABLED)
${PROMETHEUS_METRICS_SERVER}
